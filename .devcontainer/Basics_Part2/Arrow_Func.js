/*
// 1. Declaring Arrow Functions
const arrowFun = (num1, num2) => {
    return num1 + num2;
}

console.log(arrowFun(3, 7));
*/
/*
// if the function has a single variable and single statement then:
const arrowFun = num1 => (num1+=2);                                 // it automatically returns the value inside the function w/o using the return keyword
console.log(arrowFun(4));
*/

// returning objects
//const obj = () => ({name: "Divy"});              // () mandatory//
//console.log(obj());