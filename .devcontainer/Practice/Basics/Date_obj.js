/*
const date = new Date();                //returns the current time and date
console.log(date);           //the format in which date is given is not understandable and hence there are different conversion methods that we can use to make it customized
console.log(date.toString());               //returns date, time, timezone
console.log(date.toLocaleString());         //returns date, time
console.log(date.toLocaleDateString());      //returns date         
console.log(date.toLocaleTimeString());     //returns time
console.log(date.toISOString());
console.log(date.toJSON());
console.log(date.toDateString());
console.log(date.toTimeString());
console.log(date.toUTCString());
*/

//const timeInMilsecs = Date.now();
//console.log(timeInMilsecs);                          // returns time in miliseconds from 1 jan 1970

//const date = new Date();                //all the below methods will return time and date from 1 jan 1970

/*
console.log(date.getDate());
console.log(date.getTime());
console.log(date.getMonth());
console.log(date.getFullYear());
console.log(date.getDay());
console.log(date.getHours());
console.log(date.getMinutes());
console.log(date.getSeconds());
console.log(date.getMilliseconds());
console.log(date.getTimezoneOffset());
*/

//customizing methods
/*
date.toLocaleString('default', {
    weekday: "long",
    year: "2-digit",                //and many more properties we can access
});

console.log(date.toLocaleString());
*/
