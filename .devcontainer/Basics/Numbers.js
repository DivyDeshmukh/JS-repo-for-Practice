/*
// 1. Declaring Numbers
const score1 = 3434;
const score2 = new Number(675);

//console.log(score1);
//console.log(score2);

// 2. Working with methods
//console.log(score2.toString());                             // converts score2 to a string temporarily
console.log(typeof score2.toString());

//console.log(typeof score2.toLocaleString());                   // converts number to a string acc. to the local settings temporarily

console.log(typeof score1);
*/